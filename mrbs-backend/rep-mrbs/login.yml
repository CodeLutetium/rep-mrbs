info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
