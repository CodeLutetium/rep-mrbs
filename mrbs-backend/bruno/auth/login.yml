info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: mkoh028
      - name: password
        type: text
        value: "1234"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
