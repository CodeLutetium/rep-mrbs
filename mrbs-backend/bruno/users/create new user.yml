info:
  name: create new user
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/users/new
  body:
    type: json
    data: |-
      {
        "users": "#Dwayne Johnson# jdson@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
